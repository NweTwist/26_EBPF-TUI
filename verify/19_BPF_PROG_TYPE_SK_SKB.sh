#!/bin/bash
# ═══════════════════════════════════════════════════════════════
# Модуль 19: BPF_PROG_TYPE_SK_SKB
# Назначение: stream verdict для сокетов в sockmap
# Хук: sk_skb/stream_verdict — решение о маршрутизации данных
# Карта: skb_count (счётчик), sock_map (SOCKMAP)
# Особенность: модуль самодостаточен — сам создаёт TCP-пару
# Ожидание: skb_count увеличивается при передаче данных
# ═══════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "[VERIFY] Модуль: BPF_PROG_TYPE_SK_SKB"
echo "[VERIFY] Функция: stream verdict через sockmap"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo ""
echo "[VERIFY] Модуль самодостаточен: сам создаёт TCP-пару"
echo "[VERIFY] и передаёт данные через sockmap"
echo "[VERIFY] Внешние действия не требуются"
echo ""
echo "[VERIFY] Ожидание: skb_count увеличивается автоматически"
sleep 2
echo "[VERIFY] Пауза 2с для накопления событий"

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "[VERIFY] Итог: модуль работает автономно"
echo "[VERIFY] Проверьте в [RT] что skb_count > 0 и растёт"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
exit 0
