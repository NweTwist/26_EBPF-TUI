#!/bin/bash
# ═══════════════════════════════════════════════════════════════
# Модуль 17: BPF_PROG_TYPE_SK_MSG
# Назначение: фильтрация сообщений в sockmap
# Хук: sk_msg — перехватывает sendmsg() для сокетов в sockmap
# Карта: msg_count (счётчик сообщений), sock_map (SOCKMAP)
# Особенность: модуль самодостаточен — сам создаёт пару сокетов
# Ожидание: msg_count увеличивается при отправке данных
# ═══════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "[VERIFY] Модуль: BPF_PROG_TYPE_SK_MSG"
echo "[VERIFY] Функция: фильтрация сообщений через sockmap"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

echo ""
echo "[VERIFY] Модуль самодостаточен: сам генерирует TCP-трафик"
echo "[VERIFY] через пару сокетов, зарегистрированных в sockmap"
echo "[VERIFY] Внешние действия не требуются"
echo ""
echo "[VERIFY] Ожидание: msg_count увеличивается автоматически"
echo "[VERIFY] при каждом sendmsg() через sockmap-сокеты"
sleep 2
echo ""
echo "[VERIFY] Пауза 2с для накопления событий"

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "[VERIFY] Итог: модуль работает автономно"
echo "[VERIFY] Проверьте в [RT] что msg_count > 0 и растёт"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
exit 0
